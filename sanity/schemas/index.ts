import aboutSchema from './about-schema';
import projectSchema from './project-schema';
import workPageSchema from './work-page-schema';

const schemas = [aboutSchema, projectSchema, workPageSchema];

export default schemas;
